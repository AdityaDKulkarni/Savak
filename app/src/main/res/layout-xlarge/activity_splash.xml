<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ivMhLibraries"
        android:layout_width="@dimen/d300dp"
        android:layout_height="@dimen/d300dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/mhlibraries"/>

    <TextView
        android:id="@+id/tvAppName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:background="@color/colorPrimary"
        android:gravity="center"
        android:textSize="@dimen/s76sp"
        android:layout_below="@id/ivMhLibraries"/>

    <ImageView
        android:id="@+id/ivTantraved"
        android:layout_width="@dimen/d125dp"
        android:layout_height="@dimen/d125dp"
        android:src="@drawable/tantraved_logo"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
